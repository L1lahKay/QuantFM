apiVersion: batch/v1
kind: Job
metadata:
  name: bare-holder-5gpu
  namespace: khalil-scheduler-bench
  labels:
    owner: khalil
    experiment: bare-contention
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 120
  template:
    metadata:
      labels:
        owner: khalil
        experiment: bare-contention
        role: holder
    spec:
      restartPolicy: Never
      runtimeClassName: nvidia
      nodeSelector:
        accelerator: nvidia
      terminationGracePeriodSeconds: 0
      containers:
        - name: holder
          image: ubuntu:22.04
          imagePullPolicy: IfNotPresent
          command: ["/bin/bash", "-lc"]
          args:
            - |
              set -euo pipefail
              echo "HOLDER_START $(date -u +%Y-%m-%dT%H:%M:%SZ)"
              echo "VISIBLE_GPUS $(nvidia-smi -L | wc -l)"
              sleep 30
              echo "HOLDER_END $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
              nvidia.com/gpu: "5"
            limits:
              cpu: 500m
              memory: 256Mi
              nvidia.com/gpu: "5"
