# OrderFlow FM 阶段进展

---

## 一、本阶段目标与结论


| 目标                | 结论                                              |
| ----------------- | ----------------------------------------------- |
| 搭建 MinIO 读写 Infra | **已完成**：形成「读原始行情 → 本地加工 → 写回训练产物」的标准流水线         |
| 跑通小 / 中 / 大模型     | **小、中已跑通**；**~302M 正式管道已启动**（22 日全市场数据生产 + 断点续跑/并行清洗）；60 日更大档仍可一键扩展 |


**一句话**：本阶段把「能连上数据」升级成「可复用的数据 Infra」，并验证了从微型验证到真实规模预训练的整条技术链路；当前重点是加速全市场数据生产并完成 300M 从头预训练。

---

## 二、MinIO 读写 Infra（做了什么）

### 2.1 解决的核心问题

前期阻塞主要来自环境与路径，而非模型本身：

- 示例地址 / 示例 bucket 与机房实际不一致
- 读端与写端端口、权限分离，需独立配置
- 原始数据体量大（约 **4 TiB**），不能整盘落地

因此本阶段重点不是「再写一个清洗脚本」，而是**把读写固化成可运维基础设施**。

### 2.2 已落地能力

```text
【读】机房 MinIO :9000 / zeus-cn-quote
        原始 L2（成交 + 委托）
              │
              ▼  Polars 流式读取 + 订单簿重建
        本地 transient 加工
        （events → tokens → vocab / manifest）
              │
              ▼
【写】机房 MinIO :9100 / model-cache
        fm-pretrain/khalil/{规模标签}/
```


| 能力项     | 说明                                    |
| ------- | ------------------------------------- |
| 读写分离    | 读 raw @ `:9000`；写 tokens/产物 @ `:9100` |
| 连通自检    | 一键检查读写 endpoint 是否可达                  |
| 上传 / 下载 | 训练产物可备份到 MinIO，也可按需拉回本机再训             |
| 一键流水线   | 支持「只做数据」或「数据 + 多卡训练」两种模式              |
| 磁盘策略    | 按日处理、本地临时落盘，避免全量原始数据占满机器              |
| 断点续跑    | 日期 `.done` / `.clean_done`；标的级跳过已有 `events.parquet`；events/tokens 文件级跳过 |
| 并行清洗    | `CLEAN_WORKERS` 多进程按标的并行订单簿重建（显著缩短全市场日清洗） |
| 训练续训    | checkpoint 含模型 + optimizer + scaler + step；`--resume auto` |




### 2.3 对业务 / 工程的价值

1. **可复现**：任何人配置密钥后，可用同一套命令复现数据制作过程
2. **可扩展**：从 5 日试跑放大到 60 日全市场，只改模式参数，不改架构
3. **可运维**：读写健康检查、产物路径规范、本地与 MinIO 双存策略清晰

---



## 三、小 / 中 / 大模型跑通（做了什么）

按「数据规模递增 → 模型规模递增」三级验证，避免一开始就上全市场大训。

### 3.1 三级定义


| 档位    | 数据规模（约定）                        | 模型量级（约）         | 当前状态                               |
| ----- | ------------------------------- | --------------- | ---------------------------------- |
| **小** | 合成数据 smoke；真实数据 pilot（少日 / 少标的） | ~5–15M          | **已跑通**（含 checkpoint、embedding）    |
| **中** | 5 交易日 × 每市场 30 股（medium_try）    | ~80–120M 档试跑    | **已跑通**（完整数据链路 + 8 卡训练 checkpoint） |
| **300M** | 22 交易日 × 沪深全市场（Chinchilla ≈ 6B train events） | ~302M（1024×18） | **数据生产进行中**；管道含并行清洗/断点续跑；训练配置与 `--resume auto` 已就绪 |
| **大** | 60 交易日 × 沪深全市场（≈ 总事件量 1/10）     | ~1B 规划档 | **管道与训练配置可扩展**；待 300M 验证后推进 |


> 说明：本项目「大」定义为相对全市场日历的约 **1/10 抽样规模**（60 日 × 全市场），而非一次性吃完 MinIO 上全部交易日。



### 3.2 各档验证内容

**小模型**

- 合成数据端到端：分词 → 预训练 → embedding → 下游 ranker / 回测闸门  
- 真实 MinIO 数据 pilot：清洗 → tokenize → 预训练 → 抽出 stock-day embedding

**中模型**

- 打通真实 MinIO 读入  
- 完成 clean / events / tokens / manifest  
- 完成 **8 卡 FSDP** 预训练，产出可用权重

**大 / 300M 模型**

- 定稿规模估算与推荐规则（按事件量选模型档位，Chinchilla \(D\approx20N\)）  
- 配齐 22 日全市场日期列表、302M 8 卡配置与 `run_minio_300m_pipeline.sh`  
- 落地并行清洗、标的/日期级断点续跑与训练态恢复  
- 后续可按同一 Infra 放大到 60 日 / 1B



### 3.3 阶段成果对照


| 检查项              | 小   | 中   | 300M    | 大（60 日） |
| ---------------- | --- | --- | ------- | ------- |
| 能从 MinIO 读到数据    | ✓   | ✓   | ✓       | ✓（同链路）  |
| 能产出标准 tokens     | ✓   | ✓   | 生产中     | 入口已备    |
| 能完成预训练           | ✓   | ✓   | 数据齐后自动训 | 配置已备    |
| 能落盘可用 checkpoint | ✓   | ✓   | 待正式开训   | 待执行 |
| 能抽取 embedding    | ✓   | ✓   | 待正式开训   | 待执行 |
| 断点续跑 / 并行清洗     | —   | 部分  | ✓       | ✓（同链路）  |


---



## 四、当前可用性与边界

**已经可用**

- MinIO 读写 Infra 与文档化操作流程  
- 小模型、中模型完整技术验证  
- 「读 → tokens → 写 → 多卡训练」一键路径  
- 300M：并行清洗 + 多级断点续跑 + 训练 `--resume auto`

**明确边界（便于对齐预期）**

- 300M 正式训练取决于 22 日全市场 tokens/manifest 是否就绪（数据生产为当前主耗时）  
- 60 日 / 1B 属后续放大项，Infra 与扩展方式已具备  
- 训练仍以**本机 tokens** 为主；MinIO 定位为数据源与产物备份，而非训练时远端随机读  

---



## 五、建议下一步（供决策）

1. **完成 300M 数据生产并开训**：`run_minio_300m_pipeline.sh`（已支持续跑）
2. **下游验证**：用 `best.pt` 抽 embedding，跑截面 RankIC / CPCV / 回测
3. **按 scaling 放大**：验证 300M 后再推进 60 日 / 1B（需 block-level FSDP + activation checkpointing）

---



## 六、附录（执行入口，供工程同事）

```bash
source ~/.minio_fm_env.sh
make check-minio

# 中档：数据 + 8 卡训练（已验证）
make minio-full-pipeline

# 300M：22 日全市场 + 并行清洗 + 断点续跑 + 自动续训
CLEAN_WORKERS=16 SKIP_UPLOAD=1 bash quant_fm/scripts/run_minio_300m_pipeline.sh

# 更大档：60 日 × 全市场 + 训练
make minio-full-pipeline-full
```

更细技术说明见：`docs/minio_setup.md`、`docs/minio_data_workflow.md`、`docs/pipeline/`。